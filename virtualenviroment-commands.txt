pip install virtualenv # Install the package
python -m venv enviromentname # creates an virtual enviroment 
source virtualenviroment/bin/activate # to activate vritual enviroment
pip freeze # Shows all the installed packages
pip freeze > requirements.txt # creates an copy of output of pip freeze
We can distribute this file to other users, and they can recreate the same environment
using:
pip install –r requirements.txt
