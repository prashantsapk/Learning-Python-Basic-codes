# Tupple are immutable
a = ("prashant",5,6,8,8)
print("The tupple is ",a)
print("Counting how many 8 we have",a.count(8)) #count how many 8 we have
print("Using index",a.index(8)) # on which index the 8 value is ? its 3.
